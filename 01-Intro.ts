let greetings:string = "Hello World!";

greetings.toUpperCase(); // Smartly Only give string methods.

// greetings = 10; -> Error: Type 'number' is not assignable to type 'string'.ts(2322)

export {} // Temporary to remove ts error.